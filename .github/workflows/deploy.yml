name: Deploy

on:
  push:
    branches:   
      - main
  pull_request:
    branches: 
      - main
    
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: NaySoeWinYoma/lambda-actions@main
        with:
          aws-key: "UIUSHFOSOSOSOW"